# KfuPet 前端

KfuPet 网站的前端，基于 Vue 3 + TypeScript + Vite + Naive UI + Tailwind CSS，包含「社区」与「模型」两大板块，账号互通。

完整说明见根目录 [README](../README.md)。

## 快速启动

```bash
npm install
npm run dev
```

默认访问 http://localhost:5173，开发环境下 `/api` 请求会代理到后端 http://localhost:3000。
