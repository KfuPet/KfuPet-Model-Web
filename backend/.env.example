# 复制本文件为 .env，并按需修改后填写真实值

# 数据库连接（默认与 docker-compose.yml 保持一致）
DATABASE_URL="postgresql://kfupet:kfupet@localhost:5432/kfupet?schema=public"

# JWT 密钥（生产环境务必更换为随机强密钥）
JWT_SECRET="change-me-to-a-random-secret"

# 服务端口
PORT=3000
